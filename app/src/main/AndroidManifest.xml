<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifsp.scl.sdm.gerenciadorfinanceirosdm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_gf"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_gf"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.NovaContaFragment"></activity>
        <activity android:name=".view.CadastraConta" />
        <activity android:name=".view.ContaFragment" />
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>